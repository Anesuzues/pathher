// This file is a placeholder for Firebase configuration.
// To integrate Firebase:
// 1. Run the `set_up_firebase` tool.
// 2. Accept the terms in the UI.
// 3. The tool will then provision your project and update this file or create firebase-applet-config.json.

/*
import { initializeApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';
import { getFirestore } from 'firebase/firestore';
import firebaseConfig from './firebase-applet-config.json';

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
export const db = getFirestore(app);
*/

export const isFirebaseEnabled = false;
